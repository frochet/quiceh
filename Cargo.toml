[workspace]
members = [ "apps", "octets", "qlog", "quiche" ]
exclude = [ "fuzz", "tools/http3_test" ]

[profile.bench]
debug = true

[profile.release]
debug = true

[profile.performance]
inherits = "release"
lto = "fat"
codegen-units = 1
incremental = false

